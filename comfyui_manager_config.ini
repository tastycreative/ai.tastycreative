[default]
# ComfyUI Manager configuration for faster cold starts
channel_url = 
git_exe = 
pip_exe = 

# Disable auto-updates and registry fetching for faster startup
auto_update = False
startup_skip_update = True
bypass_ssl = False
badge_mode = simple

# Disable registry operations that cause 5+ minute delays
channel_url_list = 
model_download_by_agent = False

# Skip custom node compatibility checks for faster startup
experimental_mode = False

# Reduce startup overhead
security_level = weak
ui_mode = simple
